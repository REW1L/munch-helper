type: object
description: Partial update. At least one of `name` or `avatarId` must be provided.
additionalProperties: false
minProperties: 1
properties:
  name:
    type: string
    minLength: 1
    description: Non-empty after trim when provided.
  avatarId:
    type: integer
